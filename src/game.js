
class Screen {
    constructor(width, height) {
        this.width = width,
        this.height = height
    }

    addNewScreen(type) {
        switch (type) {
            case gameScreen:
                
                break;

            default:
                break;
        }
    }
}

const gameScreen = new Screen(100, 50);
